package util.enums;

/**
 * this enum is used by discount to categorize discounts under different types
 * of encapsulation.
 * 
 * @author ozsan
 *
 */
public enum Category {
	ITEM, CUSTOMER, QUANTITY
}
